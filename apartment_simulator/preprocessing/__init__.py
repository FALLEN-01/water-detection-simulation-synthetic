"""
Apartment simulator preprocessing package.

Contains the offline pipeline used to generate synthetic training data and fit
the Isolation Forest + calibration artifacts consumed by the live backend.

Entry point:
- `main.py`: orchestrates data generation and training, writes artifacts
"""
